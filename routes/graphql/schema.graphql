type Article {
    id: ID!
    title: String!
    body: String!
    author: User!
}

type User {
    id: ID!
    name: String!
    email: String!
}

type Query {
    articles: [Article]! @paginate(model: "Article", type: "connection")
}